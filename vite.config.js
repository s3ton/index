import { defineConfig } from 'vite';

export default defineConfig({
  // Set the base path to the name of your repository.
  // This ensures assets like CSS and JS are correctly linked on GitHub Pages.
  base: '/index/',
});
